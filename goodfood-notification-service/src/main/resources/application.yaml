spring:
  application:
    name: goodfood-notification-service

  datasource:
    url: jdbc:mysql://goodfood.c106ugc8gvds.ap-south-1.rds.amazonaws.com:3306/goodfood_notification?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: admin
    password: Admin.12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # change to validate in production
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  kafka:
    bootstrap-servers: localhost:9092

    producer:
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      key-serializer: org.apache.kafka.common.serialization.StringSerializer

    consumer:
      group-id: notification-service
      auto-offset-reset: earliest
      enable-auto-commit: false
      # key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      # value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      # properties:
      #   spring.json.trusted.packages: "*"

    # listener:
    #   ack-mode: manual

  mail:
    host: smtp.gmail.com
    port: 587
    username: yoursfutureguide@gmail.com
    password: jwkumdqjyyyscoqu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 9000

notification:
  processing-timeout-minutes: 5

# management:
#   endpoints:
#     web:
#       exposure:
#         include: health,info,metrics,prometheus

logging:
  level:
    org.springframework.kafka: INFO
    com.goodfood: DEBUG