apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  # namespace: goodfood
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: goodfood-order-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: docker
          # env:
          #   - name: SPRING_DATASOURCE_URL
          #     value: jdbc:mysql://mysql.goodfood.svc.cluster.local:3306/goodfood_orderdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
          #   - name: SPRING_DATASOURCE_USERNAME
          #     value: root
          #   - name: SPRING_DATASOURCE_PASSWORD
          #     value: root123
          #   - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          #     value: kafka.goodfood.svc.cluster.local:9092
          #   - name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI
          #     value: http://auth-server.goodfood.svc.cluster.local:9000/oauth2/jwks
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  # namespace: goodfood
spec:
  selector:
    app: order-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
